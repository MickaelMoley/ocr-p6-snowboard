<form id="form_delete" method="post" action="{{ path('trick_delete', {'id': trick.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick.id) }}">
{#    <button class="bg-red-100 hover:bg-red-600 hover:rounded hover:shadow px-4 py-2.5 text-red-800 hover:text-red-200 rounded">Supprimer</button>#}
</form>
